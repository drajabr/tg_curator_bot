services:
  tg-curator-bot:
    build: .
    container_name: tg-curator-bot
    restart: unless-stopped
    environment:
      - BOT_TOKEN=${BOT_TOKEN}
      - BOT_API_ID=${BOT_API_ID}
      - BOT_API_HASH=${BOT_API_HASH}
      - DATA_PATH=/app/data/data.json
    volumes:
      - ./data:/app/data
